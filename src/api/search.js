import { getSEARCH_suggest } from "../common/js/axios";

export function getSearch(keywords, offset, limit, type){
    getSEARCH_list(keywords, offset, limit, type).then((res)=>{
        
        
    })

}